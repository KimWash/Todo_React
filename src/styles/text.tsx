import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  baseText: {
    fontFamily: "Cochin"
  },
  titleText: {
    fontSize: 20,
    fontWeight: "bold"
  },
  bannerText: {
    fontSize: 25,
    fontWeight: "bold",
    textAlign: "center",
  }
});

export default styles